[project]
name = "diabetes-dashboard"
version = "0.2.0"
description = "Pretrained Streamlit dashboard for diabetes prediction using FNN and Hybrid ANFIS-ANN"
readme = "README.md"
requires-python = ">=3.9"
dependencies = [
    "streamlit",
    "pandas",
    "numpy",
    "scikit-learn",
    "torch",
    "matplotlib",
    "joblib"
]
